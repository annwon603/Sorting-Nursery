using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TraitType
{
    Colors,   // Represents color traits (0)
    Patterns, // Represents pattern traits (1)
    Sizes,    // Represents size traits (2)
    Texture   // Represents texture traits (3)
}
